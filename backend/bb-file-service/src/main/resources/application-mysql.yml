spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/bb_file?useUnicode=true&characterEncoding=utf8&connectionCollation=utf8mb4_unicode_ci&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: ${MYSQL_PASSWORD:12345}
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect

bb:
  file:
    upload-dir: data/mysql-uploads
