server:
  port: 9002

spring:
  application:
    name: cm-product
  datasource:
    url: jdbc:mysql://localhost:3306/campus_product?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ngsbdwhQS914!!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        enabled: true

jwt:
  secret: campusMarketSecretKey2024VeryLongSecretKeyForHS512Algorithm

internal:
  token: campus-market-internal-secret-2024

file:
  upload-dir: ./uploads

saga:
  reserve:
    timeout-minutes: 30

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.campus.market: DEBUG

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /doc.html

feign:
  client:
    config:
      default:
        connectTimeout: 2000
        readTimeout: 5000

# 智谱 AI 配置
ai:
  zhipu:
    api-key: dfd06e65ed2440aea53c266e5fce10fe.yPUd5R7rY8ZZd218
    model: glm-4-plus
