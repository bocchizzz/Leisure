server:
  port: 9003

spring:
  application:
    name: cm-trade
  datasource:
    url: jdbc:mysql://localhost:3306/campus_trade?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ngsbdwhQS914!!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        enabled: true

jwt:
  secret: campusMarketSecretKey2024VeryLongSecretKeyForHS512Algorithm

internal:
  token: campus-market-internal-secret-2024

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.campus.market: DEBUG

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /doc.html

feign:
  client:
    config:
      default:
        connectTimeout: 2000
        readTimeout: 5000
